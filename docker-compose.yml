services:
  web:
    build: .
    container_name: eli5-explainer
    network_mode: host
    volumes:
      - db-data:/app/data
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    restart: unless-stopped

volumes:
  db-data:
    driver: local
